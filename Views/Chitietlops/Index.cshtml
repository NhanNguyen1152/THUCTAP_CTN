@model IEnumerable<thuctap_CTN.Models.Chitietlop>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.L)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hv.HvId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.L.LId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CtlId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CtlId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CtlId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
